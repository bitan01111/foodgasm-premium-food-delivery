// app.js (module)
// During the next step we will move logic from foodgasm_fixed.html into modules.

import { initApp } from './modules/init.js';

window.addEventListener('DOMContentLoaded', () => {
  initApp();
});

