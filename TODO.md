# TODO

- [x] Remove hardcoded Supabase keys from `index.html` (CONFIG.SUPABASE_URL / SUPABASE_ANON_KEY)
- [x] Ensure `index.html` uses runtime config (`window.ENV`) via `assets/js/config.js`
- [x] Add/adjust Vercel security headers in `vercel.json` (CSP etc.)

- [x] Add GitHub Actions workflow to auto-deploy to Vercel on push

- [ ] Verify local build via simple smoke test (open `index.html` and ensure config loads)


